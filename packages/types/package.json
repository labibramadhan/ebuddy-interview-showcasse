{
  "name": "@ebuddy/types",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "class-validator": "^0.14.2"
  }
}
